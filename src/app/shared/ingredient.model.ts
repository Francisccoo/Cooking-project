export class Ingredient {
	
	//short and quick method to declarate-pass variables, inside constructor
	constructor(public name: string, public amount: number) {
		this.name = name;
		this.amount = amount;		
	}
}